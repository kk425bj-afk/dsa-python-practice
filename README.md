# Python DSA Practice

This repository contains my Python practice programs for Data Structures and Algorithms (DSA).

## Programs

1. **sum_of_digits.py**  
   Calculates the sum of digits of a number.

2. **closest_number_divisible.py**  
   Finds the number closest to n that is divisible by m.

## How to Run

Run the Python files using:

```bash
python sum_of_digits.py
python closest_number_divisible.py